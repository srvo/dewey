# Configuration for script migration
project_root: "src/dewey"

module_paths:
  core: "core/"
  llm: "llm/"
  pipeline: "pipeline/"
  ui: "ui/"
  utils: "utils/"
  accounting: "core/accounting/"

file_patterns:
  - \.py$
  - \.ipynb$

llm_settings:
  model: "gemini-1.5-flash"
  temperature: 0.3
  max_tokens: 4000
