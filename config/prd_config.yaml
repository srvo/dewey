---
prd:
  base_path: "config/prd"
  active_prd: "active_prd.yaml"
  schema:
    components:
      - name: str
        purpose: str 
        dependencies: list[str]
        interfaces: list[str]
    decisions:
      - timestamp: datetime
        description: str
        alternatives: list[str]
        rationale: str
  references:
    conventions: "../.aider/CONVENTIONS.md"
    codebase_analysis: "../docs/codebase_analysis.md"
